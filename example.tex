% ============================================================
%  example.tex
%  The following section contains minimal examples for common
%  IEEE TVCG elements such as figures, tables, equations and
%  references. These examples are for template purposes only
%  and should be removed before final submission.
% ============================================================


% ── Figure ──────────────────────────────────────────────────
\blindtext
\begin{figure}[!t]
    \centering
    \includegraphics[width=\columnwidth]{figures/example.png}
    \caption{A single column figure example.}
    \label{fig:example}
\end{figure}

% ── Figure spanning both columns ────────────────────────────
\blindtext
\begin{figure*}[!t]
    \centering
    \includegraphics[width=\textwidth]{figures/example_wide.png}
    \caption{A two column figure example.}
    \label{fig:example_wide}
\end{figure*}

% ── Figure side by side ────────────────────────────
\blindtext
\begin{figure}[!t]
    \centering
    \subfloat[\label{fig:left}]{
        \includegraphics[width=0.45\columnwidth]{figures/example.png}
    }
    \hfil
    \subfloat[\label{fig:right}]{
        \includegraphics[width=0.45\columnwidth]{figures/example.png}
    }
    \caption{Two figures side by side.}
    \label{fig:sidebyside}
\end{figure}

% ── Figure side by side spanning both columns ────────────────
\blindtext
\begin{figure*}[!t]
    \centering
    \subfloat[\label{fig:wide_left}]{
        \includegraphics[width=0.45\textwidth]{figures/example.png}
    }
    \hfil
    \subfloat[\label{fig:wide_right}]{
        \includegraphics[width=0.45\textwidth]{figures/example.png}
    }
    \caption{Two column subfigures spanning full width.}
    \label{fig:wide_sidebyside}
\end{figure*}

% ── Table ───────────────────────────────────────────────────
\blindtext
\begin{table}
    \caption{An Example Table}
    \label{tab:example}
    \centering
    \begin{tabular}{|c|c|c|}
        \hline
        Column A & Column B & Column C \\
        \hline
        Value 1  & Value 2  & Value 3  \\
        \hline
        Value 4  & Value 5  & Value 6  \\
        \hline
    \end{tabular}
\end{table}

% ── Equation ────────────────────────────────────────────────
\blindtext
\begin{equation}
    y = mx + b
    \label{eq:example}
\end{equation}

% ── Inline Code ─────────────────────────────────────────────
\blindtext
The function \texttt{SomeFunction()} is called here.

% ── Code Listing ────────────────────────────────────────────
\blindtext
\begin{lstlisting}[language=C++,
                   caption={An example code listing.},
                   label={lst:example}]
void SomeFunction() {
    // This is a comment
    int x = 42;
}
\end{lstlisting}

% ── References ──────────────────────────────────────────────
\noindent The following examples demonstrate cross-referencing in IEEE style:

\begin{itemize}
    \item As shown in \cref{fig:example}, the results indicate...
    \item As defined in \cref{eq:example}, the formula shows...
    \item See \cref{tab:example} for an overview.
    \item This has been demonstrated in prior work \cite{test}.
    \item If you need multiple citations, you can use \cite{test, test}.
    \item As shown in listing \ref{lst:example}, the function...
    \item As shown in \cref{fig:sidebyside}, the two figures are side by side.
    \item The left subfigure \cref{fig:left} shows the first example, while the right 
    subfigure \cref{fig:right} shows the second.
    \item As shown in \cref{fig:wide_sidebyside}, the full width figure
    spans both columns. 
    \item The left subfigure \cref{fig:wide_left} shows
    the first example, while the right subfigure \cref{fig:wide_right}
    shows the second.
\end{itemize}
\blindtext