% ============================================================
%  packages.tex
%  All packages – load here, nowhere else
% ============================================================

% --- Language Support ----------------------------------------
\usepackage[
    english,
    ngerman
    ]{babel}                    % Language support

% --- Placeholder ---------------------------------------------
\usepackage{blindtext}          % Filler text (Lorem ipsum)

% --- Math & Symbols ------------------------------------------
\usepackage{amsmath,amsfonts}   % Formulas, matrices, symbols
\usepackage{algorithmic}        % Pseudocode environment

% --- Tables --------------------------------------------------
\usepackage{array}              % Extended column types
\usepackage{stfloats}           % Floats across columns

% --- Figures & Graphics --------------------------------------
\usepackage{graphicx}           % \includegraphics
\usepackage[caption=false,
            font=normalsize,
            labelfont=sf,
            textfont=sf]{subfig} % Subfigures (a), (b), ...

% --- Colors --------------------------------------------------
\usepackage[dvipsnames]{xcolor} % Color names & custom colors

% --- Text & Special Characters -------------------------------
\usepackage{textcomp}           % Special characters (°, ™, ...)
\usepackage{url}                % \url{...} for links
\usepackage{verbatim}           % Code blocks, comment areas
\usepackage{microtype}          % Improved typesetting

% --- Column Balancing ----------------------------------------
\usepackage{balance}            % Balance columns on last page

% --- BibTeX Logo (do not modify) -----------------------------
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

% --- Cross References ----------------------------------------
\usepackage{hyperref}           % Clickable links & PDF metadata
\usepackage{cleveref}           % Smart references (\cref{})